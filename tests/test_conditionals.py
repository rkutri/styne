import pytest

@pytest.mark.skip(
    reason="ConditionalLatentLikelihood is excluded from the v0.2.0 "
    "frozen API, no test coverage for this release.")
def test_conditional_likelihood_todo():
    pass
